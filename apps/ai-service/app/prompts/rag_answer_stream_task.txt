任务：基于知识片段和上下文，直接回答用户问题。
要求：
1. 只输出给用户看的自然语言正文，不要输出 JSON、标题包裹符、代码块或额外解释。
2. 回答结构固定为四段：直接回答、为什么这么建议、和你今天的关系、下一步。
3. 只使用提供的知识片段和上下文；如果证据弱，要明确提示这是保守近似建议。
4. 语言简洁具体，优先可执行，不要展开成医疗诊断。

上下文：
```json
${context_json}
```
