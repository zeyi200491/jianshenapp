{
  "name": "@campusfit/ai-service",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8001",
    "build": "python -m compileall app",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8001",
    "lint": "python -m compileall app",
    "test": "python -m pytest"
  }
}
