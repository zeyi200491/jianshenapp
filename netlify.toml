[build]
command = "corepack enable && pnpm install --frozen-lockfile --shamefully-hoist && pnpm --filter @campusfit/web build"
publish = "apps/web/.next"

[build.environment]
NODE_VERSION = "22"
NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
package = "@netlify/plugin-nextjs"
